[project]
name = "cachemedicpp-research-repo"
version = "1.1.0"
description = "System-of-record research repository for CacheMedic++ (specs, configs, reproducibility, paper)."
requires-python = ">=3.10"

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
select = ["E", "F", "I"]
